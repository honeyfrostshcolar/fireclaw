# Tool Definitions

Place atomic, typed Agent-callable operations here. Each Tool should declare
its schema, permissions, safety metadata, resource locks, timeout, completion
evidence, and trusted Adapter binding.

Do not call each Tool a separate Skill.
