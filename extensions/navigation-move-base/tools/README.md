# Navigation Tools

Atomic navigation operations belong here:

- current: `navigate_to_point`;
- future, after contracts and policy are implemented:
  `get_navigation_status` and `cancel_navigation`.

These are Tools used by the Navigation Skill, not separate Skills.
